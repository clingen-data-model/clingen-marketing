var options = {
  valueNames: [ 'title', 'group', 'date', 'type' ]
};
var userList = new List('list_documentation_table', options);

var options = {
  valueNames: [ 'title', 'group', 'date', 'type' ]
};
var userList = new List('list_tool_table', options);
